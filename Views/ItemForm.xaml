<Window x:Class="Exchange_appl.Views.ItemForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Exchange_appl.Converters"
        mc:Ignorable="d"
        Title="Деталі товару" Height="600" Width="900">
    <Window.Resources>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="White"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="Black"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Label Style -->
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <!-- TextBox Style with Rounded Corners -->
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style with Rounded Corners -->
        <Style TargetType="ComboBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="MinHeight" Value="30"/>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="221*"/>
                <ColumnDefinition Width="219*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Деталі товару" 
                       FontSize="28" FontWeight="Bold" HorizontalAlignment="Left" Margin="345,0,0,10" Width="190"/>

            <!-- Основний контент -->
            <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Vertical" Margin="10,10,10,10">
                <TextBlock Text="Назва:" FontWeight="Bold" FontSize="16"/>
                <TextBox Text="{Binding ViewedItem.ItemName}" 
             IsReadOnly="{Binding IsEditable, Converter={StaticResource InverseBooleanConverter}}" 
             TextWrapping="Wrap"
             VerticalAlignment="Stretch"/>
                <TextBlock Text="Опис:" FontWeight="Bold" FontSize="16"/>
                <TextBox Text="{Binding ViewedItem.Description}" 
             TextWrapping="Wrap" 
             IsReadOnly="{Binding IsEditable, Converter={StaticResource InverseBooleanConverter}}" 
             VerticalAlignment="Stretch"/>
                <TextBlock Text="Категорія:" FontWeight="Bold" FontSize="16"/>
                <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding ViewedItem.Category}" 
                          IsEnabled="{Binding IsEditable}" Height="24.62"/>

                <TextBlock Text="Категорія для обміну:" FontWeight="Bold" FontSize="16"/>
                <ComboBox ItemsSource="{Binding ExchangeCategories}" SelectedItem="{Binding ViewedItem.ExchangeCategory}" 
                          IsEnabled="{Binding IsEditable}" Height="24.62"/>

                <TextBlock Text="Автор: " FontWeight="Bold" FontSize="14" Margin="0,0,0,10"  />
                <TextBlock Text="{Binding ViewedItem.AuthorPhone}" FontSize="14" Margin="0,0,0,10"  />
            </StackPanel>

            <!-- Зображення -->
            <Grid Grid.Row="1" Grid.Column="1" Margin="10,10,10,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Рамка с изображением -->
                <Border BorderBrush="Gray" BorderThickness="2" Grid.Row="0">
                    <Image Source="{Binding ViewedItem.Image}" Stretch="Uniform"/>
                </Border>

                <!-- Кнопка под рамкой -->
                <Button Content="Змінити фото" 
                        Click="ChangePhoto" 
                        Visibility="{Binding IsEditable, Converter={StaticResource BooleanToVisibilityConverter}}" 
                        Grid.Row="1" 
                        Height="40" 
                        Width="120" 
                        HorizontalAlignment="Center" 
                        Margin="0,10,0,0"/>
            </Grid>

            <!-- Секція обміну -->
            <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Vertical" Margin="10,10,10,10">
                <TextBlock Text="Ваші речі, що підходять для обміну:" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                <ComboBox ItemsSource="{Binding FilteredApplicableItems}" 
                          DisplayMemberPath="ItemName" 
                          SelectedItem="{Binding SelectedItem}" 
                          IsEnabled="{Binding IsMyItem, Converter={StaticResource InverseBooleanConverter}}" Height="20.62"/>

                <!-- Повідомлення, якщо це ваша річ -->
                <TextBlock Text="Це ваш товар. Ви не можете обміняти його." 
                           Visibility="{Binding IsMyItem, Converter={StaticResource BooleanToVisibilityConverter}}" 
                           Foreground="Red" FontSize="14"/>

                <!-- Повідомлення, якщо немає підходящих речей для обміну -->
                <TextBlock Text="{Binding ExchangeErrorMessage}" 
                           Visibility="{Binding HasApplicableItems, Converter={StaticResource InverseBooleanConverter}}" 
                           Foreground="Red" FontSize="14"/>
            </StackPanel>

            <!-- Кнопки -->
            <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0" Width="418">
                <Button Content="Зберегти зміни" Visibility="{Binding IsEditable, Converter={StaticResource BooleanToVisibilityConverter}}" 
                        Click="SaveChanges" Width="120" Height="40"/>
                <Button Content="Обміняти" Visibility="{Binding IsOfferable}" Click="OfferClick" Width="90" Height="40" Margin="10,0" />
                <Button Content="Закрити" Width="80" Height="40" Click="CloseWindow"/>
                <Button Content="Видалити" 
                        Visibility="{Binding IsMyItem, Converter={StaticResource BooleanToVisibilityConverter}}" 
                        Click="DeleteItem" Width="90" Height="40" Margin="10,0"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>
